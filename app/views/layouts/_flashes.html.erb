<% flash.each do |name, msg| %>
  <% if msg.is_a?(String) %>
    <div id="flash" class="flash-<%= name.to_s == 'notice' ? 'success' : 'error' %>">
      <span><%= msg %></span>
    </div>
  <% end %>
<% end %>
